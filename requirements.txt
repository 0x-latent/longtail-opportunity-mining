# Phase 1 核心依赖
pandas>=2.0
pyarrow>=14.0
pyyaml>=6.0
jieba>=0.42

# 测试
pytest>=7.0

# Phase 2 聚类依赖
sentence-transformers>=2.2
umap-learn>=0.5
hdbscan>=0.8
scikit-learn>=1.3
numpy>=1.24

# COS 数据同步（可选）
# cos-python-sdk-v5>=1.9

# Phase 3 LLM（可选）
# anthropic>=0.25
